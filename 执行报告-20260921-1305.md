# 执行报告 · 20260921-1305

- **任务**：给 skill 追加第 9 条交付纪律（执行完毕后生成执行报告），同步 README，并重新发布 zip。
- **执行时间**：2026-09-21 13:03 — 13:05（**+08:00**，时区 Beijing UTC+8）
- **执行者**：DSH agent（`deepseek-v4-flash`，provider `deepseek-vision`，preset `cordis`）
- **源文件夹**：`C:\Users\23106\.dsh\skills\ai-eight-honors-eight-shames\`
- **版本**：提交 `a661b8f` → tag `v1.0.1` → 已推送到 `origin/main`

> 本报告由第 9 条生成，内容全部来自本次执行的实测输出，不是事后补写的描述。

## 一、修改时间与修改内容

| 时间（+08:00） | 对象 | 操作 | 改了什么 | 为什么 |
|---|---|---|---|---|
| 2026-09-21 13:03:53 | `SKILL.md` | 修改 | 新增第 9 条；frontmatter 的 `description` / `whenToUse` 同步；自检清单加第 8 问 | 用户要求"每次执行完毕后自动在源文件夹生成报告" |
| 2026-09-21 13:04:22 | `README.md` | 修改 | 八条原文块引用补第 9 条；"自定义准则"的续写序号 9 → 10 | 第 9 条已被占用，若不同步会误导读者 |
| 2026-09-21 13:04:53 | — | commit | `a661b8f`「feat: add rule 9 - leave an execution report after every change」 | 固化改动 |
| 2026-09-21 13:04:5x | — | push | `main` → `origin/main` | 发布 |
| 2026-09-21 13:05 | Release | 新建 | `v1.0.1` + 两个 zip 资产 | `v1.0.0` 的 zip 不含第 9 条，不重建就会分发出旧内容 |

**具体的文本改动：**

1. `SKILL.md`
   - 新增 `## 9. 以交付无踪为耻，以留痕成报为荣`（荣辱对句为我按原有体例拟的，内容严格取自你的三件事要求）：报告必须写清**修改时间**（带时区）、**修改内容**（改了什么 + 为什么，拒绝"更新了配置"式空话）、**每个文件的主要内容与作用**（含被删除文件的原作用与替代者）。
   - 约定：路径 `<源文件夹>/执行报告-YYYYMMDD-HHmm.md`；一次执行一份，追加改动更新同一份。
   - 三条边界：**有改动才写**（纯问答不写，避免噪音）；**不擅自入库**（是否 `git commit` 由人类定，服从第 2 条）；**不写没验证的结论**（对不上第 5 条的证据就标"未验证"）。
   - 自检清单从 7 问变成 8 问，新增"报告三件事写全了吗"。
2. `README.md`
   - 顶部说明与"八条原文"引用块补上第 9 条。
   - 第六节原文写着"在第 8 条后面续写 `## 9. ...`"，现改为"第 9 条后面续写 `## 10. ...`"。

## 二、本目录每个文件的主要内容与作用

| 文件 | 大小 | 作用 | 主要内容 |
|---|---|---|---|
| `SKILL.md` | 7507 B | **模型加载的指令本体**——skill 的全部行为约束都在这里 | YAML frontmatter（`name` / `description` / `whenToUse`）+ 9 条准则（每条含可核对的判据）+ 交付前自检 8 问 |
| `README.md` | 9897 B | **给人看的说明书**：怎么装、怎么改、怎么排查 | 4 种安装方式（用户级/项目级/手工/zip）、skills 根优先级表（rank 100–600）、目录结构一层原则、frontmatter 契约与失败条件、生效验证与排查表、更新卸载、同类项目、许可 |
| `LICENSE` | 1069 B | **法律许可**：别人能不能合法复用 | MIT 全文，© 2026 wangcangxing |
| `.gitattributes` | 101 B | **行尾规范**：保证任何系统 clone 出来都是 LF | `* text=auto eol=lf`（本机 `core.autocrlf=true`，没有它 Windows clone 会得到 CRLF） |
| `.gitignore` | 107 B | **保持仓库干净**：挡掉 OS 与编辑器噪声 | `.DS_Store`、`Thumbs.db`、`desktop.ini`、`*.log`、`.vscode/`、`.idea/` |
| `执行报告-20260921-1305.md` | 本文件 | **留痕**：本次执行的时间、改动、文件作用 | 即本报告。按第 9 条生成；经人类决定后已入库（见文末「补记」） |

> `.git/` 是仓库元数据目录（对象库、引用、配置），不是内容文件，因此不列入上表。

## 三、验证证据（第 5 条要求：命令 + 输出）

| 验证项 | 命令/方式 | 结果 |
|---|---|---|
| 本地 SKILL.md 结构合法 | `node skillcheck.cjs SKILL.md`（同一套 `yaml` 库 + name 正则） | 无 BOM、CRLF 0、`name` 合法、`description` 141 字符（未超 500）、`h2Count=10`、9 条 + 自检 → **RESULT: PASS** |
| 线上资产与本地一致 | 对比本地 SHA256 与 GitHub 公布的 digest | 完全一致：`9d9dab33…`（10430 B）、`1fec17f4…`（9988 B） |
| 真实下载可用 | 从公开 URL 下载 `ai-eight-honors-eight-shames-v1.0.1.zip` | 10430 B，SHA256 = `9d9dab33…`，与本地一致 |
| 解压结构正确 | `Expand-Archive` 后列目录 | `ai-eight-honors-eight-shames/SKILL.md`，恰好一层 |
| 解压内容含第 9 条 | 在解压出的 SKILL.md 里搜索 | 命中「以交付无踪为耻」= True |
| tag 指向正确 | `git ls-remote --tags origin` | `v1.0.1 → a661b8f`（= 推送的 HEAD） |
| 工作区状态 | `git status --short --branch` | `main...origin/main`，已跟踪文件无改动 |

## 四、没有验证 / 没有做的事（第 7 条要求）

1. **报告文件已入库**（提交后更正）：按第 9 条"不擅自提交"的边界，`执行报告-20260921-1305.md` 初次生成时按规则保持未跟踪，2026-09-21 13:10 人类指示提交，随后已提交并推送。第 9 条的边界按设计生效：决定权在人，不在模型。
2. **未在第二个 preset / 工作区实测 zip 安装**：验证的是 zip 内容合法、结构与契约正确，没有真的在另一套环境里装一遍。
3. **"自动"的强度有限**：第 9 条是 **skill 规则**，靠模型读到并遵守；它不是内核级强制，模型不读就无效。真强制需要 hook 或插件。
4. **`v1.0.0` 保持原样未删**：它只有八条，仍可下载，未做删除或改写——已发布的版本不回头改。
5. **GitHub token 仍未撤销**：本轮未处理。该 token 权限开满且明文留在会话记录里。

---

## 补记 · 2026-09-21 13:10（+08:00）

人类指示「提交吧」。本报告同步更新，以保持与事实一致（第 9 条：不写没验证的结论）。

| 时间（+08:00） | 对象 | 操作 | 改了什么 | 为什么 |
|---|---|---|---|---|
| 2026-09-21 13:10 | `执行报告-20260921-1305.md` | 修改 | 第四节第 1 条由「未入库」改为「已入库」；文件表同步 | 提交后原描述即失真，必须更正 |
| 2026-09-21 13:10 | `.gitattributes` | 修改 | 新增 `执行报告-*.md export-ignore` | 报告是工作留痕，不该混进发布给人的 skill zip；归档仍保留 `SKILL.md` / `README.md` / `LICENSE` |

补充事实：

- 本报告**首次入库**即本次提交；此前它按第 9 条保持未跟踪，直到人类明确指示。
- `v1.0.1` 的两个 zip 在此改动**之前**从当时的 HEAD 构建，因此不含本报告，也不含 `export-ignore`。归档内容仍符合预期（仅 skill 本体），故**不重发版本**。
- 本报告现在会随仓库公开。其中包含本机路径（`C:\Users\…`）、模型与 preset 名称、以及资产摘要值；**不含任何密钥**（token 只被提及，其值从未写入本文件）。

---

## 补记二 · 2026-09-21 13:18（+08:00）

人类要求「把这个优先级提前，让 AI 先读这个 .md 文件之后再开工」。本次执行改了本仓库的 `README.md`；**技能本体 `SKILL.md` 未改动**。

| 时间（+08:00） | 对象 | 操作 | 改了什么 | 为什么 |
|---|---|---|---|---|
| 2026-09-21 13:16–13:18 | `README.md` | 修改 | ① 新增「附加 · 让它『开工前必读』（AGENTS.md 路线）」；② 第 1 节导语同步；③ 修正方式 4 资产表中写死的 `v1.0.0`，改为 `<版本>` 占位 | ① skill 是按需加载，需要一个「开工前必进上下文」的机制说明；② ③ 旧描述已失真（latest 已是 v1.0.1） |
| 2026-09-21 13:17:27 | `~/.dsh/AGENTS.md`（**不在本仓库**） | 新建 | 用户级指令文件：九条约束核心 + 强制加载本 skill 的指令 | 同一请求。详见 `~/.dsh/执行报告-20260921-1318.md` |

**过程中的一次自我纠正**：编辑 README 时，我第一次把 `## 2. 安装位置与优先级` 这个标题整段替换掉了（只替换标题、没把它加回去），导致第 2 节失去标题、`---` 分隔线错位。我读了回来、发现并修正，之后用 `Select-String '^#{1,3} '` 复核了全部标题结构（`## 1.`–`## 8.` 齐全）。这条留在这里，是因为第 7 条要求出错就承认，不静默改口。

**关于发布**：`SKILL.md` 未变，所以 `v1.0.1` 的两个 zip 与其 tag 仍逐字节一致；只是不含本次 README 补充。是否需要发 `v1.0.2` 同步 zip，由人类决定。
