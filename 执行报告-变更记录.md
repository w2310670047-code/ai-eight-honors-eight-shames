# 执行报告 · 变更记录

> **变更流水**：每次**新增或修改操作**在此追加一条，不覆盖旧条目。项目基线见 `执行报告-首次运行.md`。
> 时间一律为本机时区 **+08:00**（Beijing）。

---

## 2026-09-22 18:43 · 与 Ponytail 并存（方案 A+B）

| 项 | 内容 |
|---|---|
| **触发** | 用户：分析 [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) 并判断能否融合 → 定「A+B」，并规定两段式顺序：**先用八荣八耻确认「这事该不该做」，再用 Ponytail 确认「能不能一行做完」**；发布另行再议 |
| **改动（本仓库）** | ① `SKILL.md` 第 4 条新增「先问该不该存在（YAGNI）」一级，并指向 README 第 8 节；② 第 9 条新增「刻意的简化要留痕」边界（写明上限 + 升级触发，借鉴 `ponytail: <上限>, <升级条件>` 标记）；③ `README.md` 新增第 8 节「与配套 skill 并用：优先级与裁决」，许可顺延为第 9 节 |
| **改动（仓库外）** | ④ `~/.dsh/AGENTS.md`：第 4 条补 YAGNI 一级；新增「两段式顺序」段（含冲突裁决） |
| **改动（第三方）** | ⑤ 克隆 ponytail 到 `~/.dsh/third-party/ponytail`（提交 `e3ba2aa`）；⑥ 把 6 个 skill 装进 `~/.dsh/skills/`，并把主 skill 的 `description` 由 **826 → 444 字符**（DSH 会话目录上限 500，超限会被截断、丢掉后半段触发词） |
| **为什么** | Ponytail 管「写多少」，本准则管「怎么确认真相、怎么验证、怎么留痕」——不同的两半。合并文本会造成输出规范冲突（它要求解释 ≤3 行，我们第 9 条要求报告）与定位漂移，故采用「并存 + 显式裁决 + 少量机制借鉴」 |
| **验证** | 见文末「本次验证证据」 |
| **未做** | **未发新版本**（用户明确「后续发布后面再议」）；内容已提交推送 |

**关于第三方改动的一致性**：安装版与上游的差异**仅限 `description` 那一行**（正文 `bodyUnchanged: true`，其余 frontmatter 字段与顺序不变）。上游 `git pull` 后需重做该压缩。

**一处自我纠正（2026-09-22 18:5x）**：验证维护工具时，我想"测一下 `shorten-ponytail-description.cjs` 的守卫"，于是把它指向了**上游克隆**里的 `skills/ponytail/SKILL.md`。这是错的：脚本的守卫只校验**新描述**长度（444，合法），于是它照写，把上游副本也改成了压缩版 —— 上游克隆一度不再等于 `e3ba2aa`。已用 `git -C ~/.dsh/third-party/ponytail checkout -- skills/ponytail/SKILL.md` 恢复，并确认工作区干净、描述还原为原折叠块；安装版未受影响（仍 444 字符、ACCEPT）。教训已写进 `~/.dsh/third-party/README.md`：**该脚本没有"仅预览"模式，不要指向任何只读或上游副本**。

---

## 2026-09-21 13:29 · 报告制度改为「两份固定文件」

| 项 | 内容 |
|---|---|
| **触发** | 用户：考虑到其他用户使用的方便性，改成在工作目录的源文件夹下建立 .md 报告 —— 一份首次运行时，一份每次新增或修改操作 |
| **改动** | ① `SKILL.md` 第 9 条正文重写为两文件制（`执行报告-首次运行.md` / `执行报告-变更记录.md`），并明确「不要刷时间戳文件」；② 自检第 8 问同步；③ `README.md` 顶部引用与导语同步；④ `~/.dsh/AGENTS.md` 第 9 条同步；⑤ 新建本文件与 `执行报告-首次运行.md` |
| **为什么** | 固定文件名才能让任何人一眼找到。按日期散落的时间戳报告（如 `执行报告-20260921-1305.md`）等于没有报告 |
| **涉及文件** | `SKILL.md`、`README.md`、`执行报告-首次运行.md`（新建）、`执行报告-变更记录.md`（新建）、`~/.dsh/AGENTS.md`（不在本仓库） |
| **版本** | 发布 `v1.0.2`（zip 用 `git archive` 从新 tag 重建） |
| **未做** | `执行报告-20260921-1305.md` **保留不删**——已有留痕不该被销毁；仅在首次运行报告里标注为历史遗留 |
| **验证** | 见文末「本次验证证据」 |

---

## 2026-09-21 13:16–13:19 · 优先级提前：让它「开工前必读」

| 项 | 内容 |
|---|---|
| **触发** | 用户：把这个优先级提前，让 AI 先读这个 .md 文件之后再开工 |
| **改动** | ① 新建用户级 `~/.dsh/AGENTS.md`（九条约束核心 + 强制加载本 skill 的指令）；② `README.md` 新增「附加 · 让它开工前必读（AGENTS.md 路线）」；③ 修正方式 4 资产表中写死的 `v1.0.0`（改为 `<版本>` 占位，latest 当时已是 v1.0.1） |
| **为什么** | skill 是**按需加载**的，模型可能"没想起来"；`@deepseek-ai/dsh-agent-instructions`（本部署已挂载，`maxBytes: 65536`）会把 `$DSH_HOME/AGENTS.md` 在**每个会话的第一个请求**注入为持久基线 |
| **提交** | `1f13631` |
| **验证** | 写入后本会话**下一个模型步即注入成功**；随后修改该文件，注入内容**自动整段替换**（两次都实测到） |
| **未做** | 未在全新会话、项目级 `AGENTS.md`、以及预算溢出三种情形下实测 |

---

## 2026-09-21 13:10 · 报告入库 + 归档排除

| 项 | 内容 |
|---|---|
| **触发** | 用户：「提交吧」 |
| **改动** | ① 提交 `执行报告-20260921-1305.md`；② `.gitattributes` 新增 `执行报告-*.md export-ignore`；③ 报告内把已失真的「报告文件未入库」更正为「已入库」并加补记 |
| **为什么** | 提交后原文即失真，按第 9 条「不写没验证的结论」必须更正；报告是工作留痕，不该混进发给别人的 skill zip |
| **提交** | `74c7222` |
| **验证** | `git archive` 与 GitHub「Download ZIP」两条归档路径**均实测排除**报告，且 SKILL.md/README/LICENSE 仍在 |

---

## 2026-09-21 13:03–13:05 · 新增第 9 条

| 项 | 内容 |
|---|---|
| **触发** | 用户：每次执行完毕后自动在源文件夹生成报告，说明修改时间、修改内容、每个文件的主要内容及作用 |
| **改动** | ① `SKILL.md` 新增第 9 条「以交付无踪为耻，以留痕成报为荣」+ 自检第 8 问 + `description`/`whenToUse` 同步；② `README.md` 引用同步；③ 续写序号由 9 改为 10 |
| **提交 / 版本** | `a661b8f` / `v1.0.1` |
| **验证** | 本地结构校验 PASS（9 条 + 自检，`h2Count=10`）；zip 的 SHA256 与 GitHub digest 一致；公开 URL 下载一致；解压后含第 9 条 |

---

## 2026-09-20 15:48 · 加 MIT 许可 + zip 分发

| 项 | 内容 |
|---|---|
| **触发** | 用户：实现 zip 分发那条，用 MIT 协议 |
| **改动** | ① 新增 `LICENSE`（MIT，© 2026 wangcangxing）；② `README.md` 加「方式 4 · 下载 zip」与两种内部结构说明、许可章节改为 MIT |
| **提交 / 版本** | `054bf6c` / `v1.0.0` |
| **未验证** | 各平台对 zip 内部结构的要求**未核到官方原文**（`docs.claude.com` 已跳转迁移），故两种结构都提供 |

---

## 2026-09-20 13:21 · 仓库初始化

| 项 | 内容 |
|---|---|
| **改动** | 建 `.gitignore`、`.gitattributes`（`* text=auto eol=lf`）、`SKILL.md`、`README.md` |
| **为什么** | 本机 `core.autocrlf=true`，不锁 LF 则别处 clone 会得到 CRLF |
| **提交** | `c469771`（首次提交），随后 `3efb9a6` 把 README 里的 `<你的用户名>` 占位符换成真实 clone URL |

---

## 验证证据 · v1.0.2（2026-09-21 13:29）

| 验证项 | 方式 | 结果 |
|---|---|---|
| `SKILL.md` 契约（对**发布 zip 解压出来的副本**校验，不是对源码） | PowerShell 直接读字节 + 正则（与 provider 同一条 name 正则） | 无 BOM、首行 `---`、`name` = `ai-eight-honors-eight-shames` 合法、`description` 141 字符（未超 500）、`whenToUse` 存在、`##` 小节 10 个（9 条 + 自检）、CRLF 0、8123 B |
| 归档排除报告 | `git archive` 与公开下载 zip 的条目检查 | 报告文件条目数 = **0**；只含 `SKILL.md` / `README.md` / `LICENSE` / `.gitattributes` / `.gitignore` |
| 发布资产一致性 | 本地 SHA256 对比 GitHub 公布 digest | 完全一致：`8273bb57…`（11918 B）、`6be88e88…`（11476 B） |
| 公开下载可用 | 从 Release 公开 URL 实下 | 11918 B，SHA256 = `8273bb57…`，与本地一致 |
| 内容确为新方案 | 在解压出的 `SKILL.md` 内检索 | 含 `执行报告-首次运行.md` 与 `执行报告-变更记录.md`；**不含**旧命名 `YYYYMMDD-HHmm` |
| `AGENTS.md` 同步生效 | 观察本会话上下文 | 修改后注入内容**自动整段替换**（`Updated instructions from: ~/.dsh/AGENTS.md`） |
| 仓库同步 | `git ls-remote origin -h refs/heads/main` + `git status` | HEAD `fb99013` 与远端一致；提交后工作区干净 |
| 线上文件清单 | GitHub contents API | 8 个文件，含两份新报告 |

**一处自我纠正**：本表原先写「`SKILL.md` 结构合法｜本地校验脚本」——但改写第 9 条之后我**并没有重跑**那个契约校验，只查过标题结构。发现后补做了上面第一行的完整校验（对发布产物），结论成立；原表述属于没验证就写结论，故在此记录更正。

> 「未做」的项与「未验证」的项一律单列出来，不写成「已完成」。

---

## 验证证据 · 2026-09-22（Ponytail 并存）

| 验证项 | 方式 | 结果 |
|---|---|---|
| Ponytail 6 个 skill 是否符合 DSH 契约 | 用与 provider 同一条 name 正则 + 同一个 `yaml` 库 + legacy-key 检查逐个体检 | **6/6 ACCEPT**：无 BOM、首行 `---`、`name` 合法、无非法字段 |
| 主 skill 描述超限已修复 | 压缩脚本（内含 `>500` 守卫）跑完复检 | **826 → 444 字符**（≤500）；`name` 与字段列表未变；`bodyUnchanged: true` |
| 压缩是否只动了一行 | `Compare-Object` 上游 vs 安装版 | 行数 120 → 108；差异 14 处 = 1 增（单行 description）+ 13 删（原折叠块），**完全由该替换解释** |
| 守卫是否真的拦住了错误 | 第一次压缩尝试（新描述 640 字符） | 脚本按守卫**拒绝写入**；上游与安装版 SHA256 相同，证明文件未被改动 |
| DSH 是否真的发现了它们 | 观察本会话 skill 目录刷新 | 两次刷新：先出现 6 个 ponytail skill（主 skill 描述显示为截断的 `Al...`），压缩后描述完整、不再截断 |
| `AGENTS.md` 两段式顺序是否生效 | 观察本会话上下文 | 修改后注入内容**自动整段替换**，已含「两段式顺序」段 |
| 仓库同步 | `git ls-remote origin -h refs/heads/main` + `git status` | 与本地 HEAD 一致，提交后工作区干净（提交号见 `git log`） |

**没做的**：没有实测 Ponytail 在真实任务里的效果（要跑任务做对照）；没有复现它 README 里的 benchmark 数字；**没有发新版本**（用户明确发布事项另行再议）。
