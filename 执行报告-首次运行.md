# 执行报告 · 首次运行

```
最后一次核对时间：2026-09-22 19:04:35 +08:00
核对文件数量：8
一级目录：.git/, .gitattributes, .gitignore, LICENSE, README.md, SKILL.md,
          执行报告-20260921-1305.md, 执行报告-变更记录.md, 执行报告-首次运行.md
核心依赖文件：无（本仓库是纯文档/Markdown 仓库，没有 package.json / pyproject.toml / go.mod / Cargo.toml / requirements.txt）
```

> **防过期**：一级目录结构与本清单不符，或核心依赖文件的修改时间晚于上面的核对时间 → **必须强制局部刷新**（重新核对受影响的目录与文件、更新本清单与核对时间），并在 `执行报告-变更记录.md` 记一条；**不得按旧基线继续执行**。两条都不命中则沿用本清单，不再扫描。

> **基线清单**：进新工作区时先查找本文件；**有就以它 + `执行报告-变更记录.md` 为唯一认知基线**（读取优先级：首次运行.md ＞ 变更记录.md ＞ 实际目录结构），**严禁不必要地全量扫描**；没有才受限扫描后建立（跳过 `node_modules`/`.git`/`dist`/`build`/`.vscode` 等）。
> 首次建立：**2026-09-21 13:29（+08:00）**　|　最近核对：**2026-09-22 19:04（+08:00）**　|　维护者：DSH agent，人类维护者：wangcangxing

## 这个项目是干什么的

**AI 八荣八耻 · DSH Skill** —— 一份面向 AI 编码代理的**九条工作准则**，打包成可直接安装的 Agent Skill（`SKILL.md` 格式）。公开仓库，MIT 许可。

- 仓库：https://github.com/w2310670047-code/ai-eight-honors-eight-shames
- 本目录有**双重身份**：它既是 **DSH 本地生效的 skill**，也是**该仓库的 git 工作副本**（跟踪 `origin/main`）。改这里的文件，等于同时改本地生效内容与待发布内容。
- 安装原理：clone 或解压到任一 skills 根即可（`<skills-root>/<name>/SKILL.md`）。发现逻辑**只扫一层**。

## 文件清单：每个文件的主要内容与作用

| 文件 | 大小 | 作用 | 主要内容 |
|---|---|---|---|
| `SKILL.md` | 11811 B | **技能本体**——模型加载后照此执行的完整指令 | frontmatter（`name` / `description` / `whenToUse`）+ 第 1–9 条准则（每条含「判据」）+ 交付前自检 8 问。第 9 条含任务启动的读取优先级、受限扫描与防过期机制 |
| `README.md` | 16192 B | **给人看的说明书** | 4 种安装方式 + 「附加 · 让它开工前必读（AGENTS.md 路线）」；skills 根优先级表（rank 100–600）；一层原则；frontmatter 契约与失败条件；生效验证与排查表；更新与卸载；第 8 节「与配套 skill 并用」（Ponytail 优先级与裁决）；报告制度；同类项目；MIT 许可 |
| `LICENSE` | 1069 B | **法律许可**：别人能否合法复用 | MIT 全文，© 2026 wangcangxing |
| `.gitattributes` | 239 B | **行尾规范 + 归档排除** | `* text=auto eol=lf`（任何系统 clone 都得到 LF）；`执行报告-*.md export-ignore`（报告入库但**不进**发布 zip） |
| `.gitignore` | 107 B | **保持仓库干净** | `.DS_Store`、`Thumbs.db`、`desktop.ini`、`*.log`、`.vscode/`、`.idea/` |
| `执行报告-首次运行.md` | 本文件（大小随刷新变化） | **基线清单 + 防过期元数据** | 顶部三行元数据（最后一次核对时间 / 核对文件数量 / 一级目录）；项目定位、文件清单、关键约定、版本状态 |
| `执行报告-变更记录.md` | 15580 B | **变更流水**：用户每次提需求或提问都追加一条 | 成对精简记录：① 用户输入 ② 做出的更改；没改任何文件时写明「本次对话未对任何文件进行修改」 |
| `执行报告-20260921-1305.md` | 8801 B | **历史遗留**：迁移到「两份固定文件」制度**之前**的时间戳式报告 | 第 9 条首次执行的完整留痕。**保留不删**（不销毁既有留痕），但不再更新；其时间线已并入变更记录 |

## 关键约定（动这个项目之前先知道）

1. **一层原则**：`SKILL.md` 与 skills 根之间只能隔一层目录；嵌套的 `**/SKILL.md` 不会被发现。
2. **frontmatter 契约**：首行必须恰好是 `---`（有 BOM 即失效）；`name` 必须匹配 `^[a-z0-9]+(?:-[a-z0-9]+)*$`；`description` 在会话目录里最多渲染 500 字符；任何一条不合规，该文件会被**静默跳过**（只在 harness 日志里 warn）。
3. **报告制度**（第 9 条）：本目录**固定两份报告**，不刷时间戳文件；**用户每次提需求或提问都记**（有改动记改动，没改任何文件就写明）；任务是先读 `执行报告-首次运行.md` + 变更记录当基线，**不重复全量扫描**；是否入库由人类决定。
4. **归档排除**：`执行报告-*.md` 经 `export-ignore` 排除，所以发布 zip 只含 `SKILL.md` / `README.md` / `LICENSE` / `.gitattributes` / `.gitignore`。
5. **发布流程**：只要 `SKILL.md` 变了就发新版本 —— 用 `git archive` 从 HEAD 构建两个 zip（带目录前缀的 + flat 的），`gh release create <tag> <zip…> --notes-file <文件>`，然后**必须下载回来复验**（对比 GitHub 公布的 digest 与公开 URL 实际下载）。
6. **网络前提**：本机系统代理为 `127.0.0.1:12834`，而 **git 与 gh 默认都不读 Windows 代理设置**，所以推送/下载需注入 `HTTPS_PROXY` 与 `http.https://github.com.proxy`。本会话用进程级环境变量注入，**未改全局配置**。
7. **Token**：推送需要带 `repo` scope 的 PAT。当前使用的那枚权限开满且明文出现在会话记录里，**待人类撤销**。

## 版本与标签状态

| 标签 | 内容 | 备注 |
|---|---|---|
| `v1.0.0` | 八条版 + MIT + zip 分发 | 保留可下载，不回改 |
| `v1.0.1` | 加入第 9 条（单份时间戳报告） | 同上 |
| `v1.0.2` | 第 9 条改为**两份固定文件** | 已发布，tag → `fb99013` |
| `v1.0.3` | 报告改为**成对精简记录** + 与 Ponytail 并存 | 已发布，tag → `319ea39` |
| （待发） | 第 9 条：每次诉求都记 / 任务启动读取优先级 / 受限扫描 / 防过期刷新 | 已在 `main`，尚未发 Release |

每次变更的提交号、时间与验证证据，见 `执行报告-变更记录.md`。
